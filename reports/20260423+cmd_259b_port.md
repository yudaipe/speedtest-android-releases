# cmd_259 Wave3b 移植報告

- QAM削除箇所
  - `app/src/main/java/com/shogun/speedtest/collector/HiddenRadioCollector.kt`
  - `getDownlinkModulation()` 反射呼出と QAM 変換処理を削除
  - `app/src/main/java/com/shogun/speedtest/data/HiddenRadioSnapshot.kt`
  - `CarrierInfo` から `dlModulation` を削除
  - `app/src/main/java/com/shogun/speedtest/MainScreen.kt`
  - QAM欄を固定文言 `取得不可（機種非対応）` に変更

- band/bandwidth取得コード
  - `HiddenRadioCollector.buildCarrierInfo()` で以下を取得
  - `band` (`config.band` + `getNetworkBand()` fallback)
  - `cellBandwidthDownlinkKhz`
  - `cellBandwidthUplinkKhz`
  - `physicalCellId`
  - `networkType`
  - `frequencyRange`
  - `connectionStatus`
  - `downlinkFrequencyKhz` (`getDownlinkFrequencyKhz` / `getDownlinkFrequency` fallback)
  - `downlinkChannelNumber`
  - 取得不能値は `null` とし、UI 側で `-` 表示

- CA表示実装
  - `HiddenRadioSnapshot.ccCount` をそのまま利用
  - `MainScreen.HiddenRadioInfoCard()` ヘッダで `CA (nCC)` を表示
  - 各 CC ごとに PCC/SCC, Band, DL/UL bandwidth, PCI, channel, networkType, frequencyRange, DLFreq を表示

- arity動的検出
  - 未実施
  - `listenWithEventList` の arity 検出自体は継続
  - 実引数組立の動的分岐は未対応のため、非8引数時は fail-fast
