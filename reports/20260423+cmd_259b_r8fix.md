## 適用した対策

- 候補B: `HiddenRadioCollector#createPrivilegedRegistry()` の `SystemServiceHelper.getSystemService()` を reflection 経由から直接 import + 直呼びへ変更した。
- 候補A: `app/proguard-rules.pro` に `-keep class rikka.shizuku.** { *; }` を追加し、release ビルド時に Shizuku helper のメソッド名が難読化で崩れぬよう保護した。
- 候補C: 未適用。B+A で release ビルド成功したため、shell(2000) 権限経路の追加調査は今回は不要と判断した。

## ビルド結果

- 実行コマンド: `JAVA_HOME=/home/ysuzuki/jdk17 ./gradlew :app:assembleRelease`
- 結果: 成功
- 生成 APK: `app/build/outputs/apk/release/speed_test_monitor-v0.2.5-shizuku-beta.apk`
- 備考: `android:extractNativeLibs` 警告と `PhysicalChannelConfig.CONNECTION_*` 非推奨警告のみ。ビルド阻害なし。

## 変更箇所

- `app/src/main/java/com/shogun/speedtest/collector/HiddenRadioCollector.kt`
  - `rikka.shizuku.SystemServiceHelper` / `ShizukuBinderWrapper` を直接 import
  - `SystemServiceHelper.getSystemService(serviceName)` を直呼び
  - binder wrap 生成は `ShizukuBinderWrapper(registryBinder)` を直接使用
- `app/proguard-rules.pro`
  - `rikka.shizuku.**` keep ルール追加
- `app/build.gradle.kts`
  - `versionName = "0.2.5-shizuku-beta"`
  - `versionCode = 205`

## 非干渉確認

- `git diff main..feature/shizuku-beta -- app/src/main/java/com/shogun/speedtest/collector/CellularInfoCollector.kt`
  - 差分なし
