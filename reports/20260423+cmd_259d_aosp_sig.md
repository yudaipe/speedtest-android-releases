## cmd_259d AOSP signature fix

- Approach: B (existing manual binder path corrected to AOSP-confirmed 8-argument order)
- AIDL placed: false
- Reason:
  - `IPhoneStateListener` full hidden AIDL has broad transitive parcelable dependencies.
  - The task explicitly allowed fallback to approach B if AIDL path was risky.
  - The existing binder callback path was already in place, so the lowest-risk fix was to correct argument order and event ID.

## Code changes

- `HiddenRadioCollector.kt`
  - `listenWithEventList` write order fixed to 8 arguments:
    1. `false`
    2. `false`
    3. `SubscriptionManager.getDefaultSubscriptionId()`
    4. `context.packageName`
    5. `context.attributionTag ?: ""`
    6. callback binder
    7. `intArrayOf(19)`
    8. `true`
  - `listenArity` now rejects non-8 variants for this path.
  - `EVENT_PHYSICAL_CHANNEL_CONFIG_CHANGED` corrected from `33` to `19`.
- `app/build.gradle.kts`
  - `versionName = "0.2.7-shizuku-beta"`
  - `versionCode = 207`

## Build result

- Command:
  - `export JAVA_HOME=/home/ysuzuki/jdk17 && ./gradlew :app:assembleRelease`
- Result: success
- Notes:
  - Initial build attempt failed because `JAVA_HOME` was unset.
  - Re-run with `/home/ysuzuki/jdk17` succeeded.
  - Remaining warnings were existing deprecation warnings around `PhysicalChannelConfig.CONNECTION_*`.

## Non-interference check

- `git diff main..feature/shizuku-beta -- app/src/main/java/com/shogun/speedtest/collector/CellularInfoCollector.kt`
- Result: no diff
